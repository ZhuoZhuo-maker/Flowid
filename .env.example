# 开发默认：积分 SQLite API 与 `/pts/admin` 管理页已并入 Auth 服务（npm run auth:dev / npm run dev，端口 AUTH_SERVER_PORT 默认 3721）。
# 未单独设置 ADMIN_TOKEN 时，管理页/API 的 x-admin-token 可与 AUTH_ADMIN_SECRET 一致。

# 仅当单独运行「纯积分进程」时使用（node src/backend/server.js，默认 3001，路径仍为 /pts）：
# PORT=3001

# SQLite 文件路径（相对仓库根目录或绝对路径）
DB_PATH=./data/flowid.db

# 可选：单独覆盖积分管理口令；不设则回退 AUTH_ADMIN_SECRET
# ADMIN_TOKEN=your-secret-token

# 积分预扣 /api/points/reserve 与 Auth 会员联动（可选）：逗号分隔 nodeKind，例如 image,video
# 非空时，对应节点预扣需在请求体附带 authLicenseCode + authMachineId（Auth JWT 明文码与机器码），且 Auth 返回 proTemplates。
# POINTS_PRO_MEMBERSHIP_NODE_KINDS=image,video
# 与 Auth 不同机或端口非 3721 时，覆盖 verify 地址：
# AUTH_INTERNAL_LICENSE_VERIFY_URL=http://127.0.0.1:3721/license/verify

# --- 上线（生产）与「钱」相关的要点 ---
# 1) 真钱数据在 SQLite 文件里（DB_PATH），与浏览器 localStorage 无关。须定期冷备/快照该文件（及 WAL 同目录下的 -shm/-wal），
#    并限制只有运维账号能读盘；丢库才是丢余额与流水。
# 2) 生产务必设置 NODE_ENV=production，并设置强随机 ADMIN_TOKEN（或 AUTH_ADMIN_SECRET）；勿使用默认 flowid-admin-dev。
#    管理页把口令记在 localStorage 仅为操作员本机方便，清缓存后重填即可，不会删库里的授权码。
# 3) 对外只暴露 HTTPS；/pts/admin 建议仅内网或 VPN 可达，或再加一层网关鉴权。
# 4) 发码、改分、批量补偿等能力等价于资金操作，只给可信人员；审计可查 points_log 与 licenses 表。

# --- 桌面端打包（pack-flowid-desktop.bat）会注入，一般不必手写 ---
# VITE_FLOWID_PUBLIC_SERVER_ORIGIN=https://你的域名
# VITE_LICENSE_API_URL=https://你的域名/pts
# 授权页「交流群」：仅 QQ 群号数字（分享链接会过期，勿填 URL）
# VITE_FLOWID_EXCHANGE_GROUP_QQ=1103016040

# 为 1 时：预设模板与灵感小镇从 public/flowid-bundled/ 随包读取，不向授权服务拉列表/封面（见 public/flowid-bundled/README.txt）
# VITE_FLOWID_LOCAL_GALLERY=1
# 开发时可复制为 .env.development.local（仓库已提供示例文件，git 忽略 *.local）

# 导出 flowid-bundled：默认 http://127.0.0.1:3721（先 npm run auth:dev）；npm run export:bundled-gallery
# 可选覆盖：FLOWID_EXPORT_GALLERY_BASE=https://host:3721

# 敏感词合并上限（build:lexicon），默认 2800；0=不裁剪。词少则 AC 构建更快。
# FLOWID_LEXICON_MAX=2500
